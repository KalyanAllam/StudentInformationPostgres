@model IEnumerable<SISPostgres.Models.Faculty>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Facultyfirstname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Facultylastname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Facultyfirstname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Facultylastname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Facultyid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Facultyid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Facultyid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
